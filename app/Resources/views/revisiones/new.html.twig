{% extends 'base.html.twig' %}

{% block body %}
    <h1>Revisione creation</h1>

    {{ form_start(form, {'enctype':'multipart/form-data'}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('revisiones_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
