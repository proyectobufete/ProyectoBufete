{% extends 'base.html.twig' %}

{% block body %}
    <h1>Revisione edit</h1>

        <a href="{{ asset ('uploads/final/'~revisione.ruta) }}" download>
            <img src="{{ asset ('uploads/final/pdf.png')}}" width="28" height="30">
            {{ asset (revisione.ruta) }}
        </a>

    {{ form_start(edit_form, {'enctype':'multipart/form-data'}) }}
        {{ form_widget(edit_form) }}


        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('revisiones_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
