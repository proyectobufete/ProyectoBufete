{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tipoparcticante</h1>

    <table>
        <tbody>
            <tr>
                <th>Idtipopracticante</th>
                <td>{{ tipoparcticante.idtipopracticante }}</td>
            </tr>
            <tr>
                <th>Tipopracticante</th>
                <td>{{ tipoparcticante.tipopracticante }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipoparcticante_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipoparcticante_edit', { 'idtipopracticante': tipoparcticante.idtipopracticante }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
